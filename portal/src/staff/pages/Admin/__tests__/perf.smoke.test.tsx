import { describe, expect, it } from 'vitest';

import AdminPerf from '../Perf';

describe('AdminPerf', () => {
  it('is a function component', () => {
    expect(typeof AdminPerf).toBe('function');
  });
});
